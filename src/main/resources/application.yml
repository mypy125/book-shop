spring:
  application:
    name: my-shop-book

  datasource:
    url: jdbc:postgresql://172.18.0.1:5432/bookstore
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
      dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  error:
    include-message: ALWAYS